<%= form_for(@sla_per_month) do |f| %>
  <% if @sla_per_month.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sla_per_month.errors.count, "error") %> prohibited this sla_per_month from being saved:</h2>

      <ul>
      <% @sla_per_month.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :month %><br>
    <%= f.text_field :month %>
  </div>
  <div class="field">
    <%= f.label :year %><br>
    <%= f.number_field :year %>
  </div>
  <div class="field">
    <%= f.label :customer_id %><br>
    <%= f.number_field :customer_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
